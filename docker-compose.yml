version: '3.4'

services:
  settlementautomationapi:
    image: settlementautomationapi
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 203:203
